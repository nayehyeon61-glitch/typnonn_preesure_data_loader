name: tests

on:
  push:
    branches:
      - main
      - feature/weathernext-resolver
  pull_request:

jobs:
  pytest:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip
      - name: Install test dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e '.[test,gpt,flow]'
      - name: Run tests
        run: pytest -q
